// const baseCurrencyUs = 25.25;

// //helper
// function roundTwoDecimals(amount) {
// 	return Math.round(amount * 100) / 100;
// };

// module.exports.convertToUa = function convertToUa(amount) {
// 	return roundTwoDecimals(amount * baseCurrencyUs).toFixed(2);
// };

// module.exports.convertToUs = function convertToUs(amount) {
// 	return roundTwoDecimals(amount / baseCurrencyUs).toFixed(2);
// };

// -------------------------------------


// function Converter(baseCurrencyUs) {
// 	this.baseCurrencyUs = baseCurrencyUs;
// };

// Converter.prototype.roundTwoDecimals = function(amount) {
//     return Math.round(amount * 100) / 100;
// };

// Converter.prototype.convertToUa = function(amount) {
//     return this.roundTwoDecimals(amount * this.baseCurrencyUs).toFixed(2);
// };

// Converter.prototype.convertToUs = function(amount) {
//     return this.roundTwoDecimals(amount / this.baseCurrencyUs).toFixed(2);
// };

// module.exports = Converter;

//-----------------------


// class Converter {
//   constructor(baseCurrencyUs) {
//     this.baseCurrencyUs = baseCurrencyUs;
// 	 }
// 	 roundTwoDecimals(amount) {
//     return Math.round(amount * 100) / 100;
//   }
//   convertToUa(amount) {
//     return this.roundTwoDecimals(amount * this.baseCurrencyUs).toFixed(2);
//   }
//   convertToUs(amount) {
//     return this.roundTwoDecimals(amount / this.baseCurrencyUs).toFixed(2);
//   }
// }

// module.exports = Converter;

// -------------------------------

class User 







